import {supabase} from '@/lib/supabase';
export async function toggleReelLike(reelId:string,liked:boolean){const {data:{user}}=await supabase.auth.getUser(); if(!user) throw new Error('Authentication required'); if(liked){const {error}=await supabase.from('reel_likes').delete().eq('reel_id',reelId).eq('user_id',user.id); if(error)throw error; return false;} const {error}=await supabase.from('reel_likes').insert({reel_id:reelId,user_id:user.id}); if(error&&error.code!=='23505')throw error; return true;}
export async function toggleSaved(table:'saved_reels'|'saved_prompts',reelId:string,saved:boolean){const {data:{user}}=await supabase.auth.getUser();if(!user)throw new Error('Authentication required');if(saved){const {error}=await supabase.from(table).delete().eq('reel_id',reelId).eq('user_id',user.id);if(error)throw error;return false;}const {error}=await supabase.from(table).insert({reel_id:reelId,user_id:user.id});if(error&&error.code!=='23505')throw error;return true;}
export async function toggleFollow(userId:string,following:boolean){const {data:{user}}=await supabase.auth.getUser();if(!user)throw new Error('Authentication required');if(user.id===userId)throw new Error('You cannot follow yourself');if(following){const {error}=await supabase.from('follows').delete().eq('follower_id',user.id).eq('following_id',userId);if(error)throw error;return false;}const {error}=await supabase.from('follows').insert({follower_id:user.id,following_id:userId});if(error&&error.code!=='23505')throw error;return true;}
export async function copyPrompt(reelId:string,prompt:string){await navigator.clipboard.writeText(prompt);const {data:{user}}=await supabase.auth.getUser();if(user){const {error}=await supabase.from('prompt_copies').insert({reel_id:reelId,user_id:user.id});if(error&&error.code!=='23505')throw error;}return true;}
