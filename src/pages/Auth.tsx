import React,{useState} from 'react';import {useLocation} from 'wouter';import {supabase} from '@/lib/supabase';import {useToast} from '@/hooks/use-toast';import {Sparkles} from 'lucide-react';
export default function Auth({mode='login'}:{mode?:'login'|'signup'}){const [,setLocation]=useLocation();const {toast}=useToast();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [name,setName]=useState('');const [busy,setBusy]=useState(false);const submit=async(e:React.FormEvent)=>{e.preventDefault();setBusy(true);const r=mode==='login'?await supabase.auth.signInWithPassword({email,password}):await supabase.auth.signUp({email,password,options:{data:{full_name:name}}});setBusy(false);if(r.error){toast({title:'Authentication failed',description:r.error.message,variant:'destructive'});return}toast({title:mode==='login'?'Welcome back':'Account created'});setLocation('/reels')};return <main className="grid min-h-screen place-items-center bg-[#050609] p-5 text-white"><form onSubmit={submit} className="w-full max-w-md rounded-[30px] border border-white/10 bg-white/[.04] p-7 backdrop-blur-xl"><div className="mb-7"><div className="mb-3 inline-flex rounded-2xl bg-gradient-to-br from-cyan-300 to-blue-600 p-3 text-black"><Sparkles/></div><h1 className="text-3xl font-black">{mode==='login'?'Welcome back':'Create your creator account'}</h1><p className="mt-2 text-sm text-white/45">Prompt-first short videos, recipes, and creators.</p></div>{mode==='signup'&&<input value={name} onChange={e=>setName(e.target.value)} placeholder="Display name" className="mb-3 w-full rounded-xl border border-white/10 bg-black/30 p-3 outline-none"/>}<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email" className="mb-3 w-full rounded-xl border border-white/10 bg-black/30 p-3 outline-none"/><input required minLength={6} type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password" className="mb-4 w-full rounded-xl border border-white/10 bg-black/30 p-3 outline-none"/><button disabled={busy} className="w-full rounded-xl bg-cyan-400 p-3 font-black text-black disabled:opacity-50">{busy?'Working…':mode==='login'?'Sign in':'Sign up'}</button><button type="button" onClick={()=>setLocation(mode==='login'?'/signup':'/login')} className="mt-4 w-full text-sm text-white/50">{mode==='login'?'Need an account? Sign up':'Already have an account? Sign in'}</button></form></main>}
