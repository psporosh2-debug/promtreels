import React,{useEffect,useState} from 'react';import {Link} from 'wouter';import {supabase} from '@/lib/supabase';import {Trophy,UserCircle} from 'lucide-react';
export default function LeaderboardPage(){const [tab,setTab]=useState('top');const [rows,setRows]=useState<any[]>([]);useEffect(()=>{(async()=>{const {data}=await supabase.rpc('get_leaderboard',{p_metric:tab,p_limit:50});setRows(data||[])})()},[tab]);return <main className="min-h-screen bg-[#050609] p-4 pb-24 text-white md:pl-24"><div className="mx-auto max-w-4xl pt-8"><div className="flex items-center gap-3"><Trophy className="text-cyan-300"/><h1 className="text-3xl font-black">Creator Leaderboard</h1></div><div className="mt-5 flex flex-wrap gap-2">{[['top','Top Creators'],['copies','Most Copied'],['saves','Most Saved'],['likes','Most Liked'],['rising','Rising']].map(([id,n])=><button key={id} onClick={()=>setTab(id)} className={`rounded-full px-4 py-2 text-xs font-bold ${tab===id?'bg-cyan-400 text-black':'bg-white/5 text-white/55'}`}>{n}</button>)}</div><div className="mt-6 space-y-2">{rows.map((r,i)=><Link key={r.id||i} href={`/profile/${r.username}`}><a className="flex items-center gap-4 rounded-2xl border border-white/10 bg-white/[.035] p-4"><div className="w-8 text-center font-black text-cyan-300">#{i+1}</div><UserCircle className="h-10 w-10"/><div className="flex-1"><div className="font-black">{r.display_name||r.username}</div><div className="text-xs text-white/40">@{r.username}</div></div><b>{Number(r.metric_value||0).toLocaleString()}</b></a></Link>)}{rows.length===0&&<div className="py-20 text-center text-white/40">Leaderboard data will appear as creators publish and engage.</div>}</div></div></main>}
