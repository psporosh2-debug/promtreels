import React,{useEffect,useState} from 'react';import {supabase} from '@/lib/supabase';import {BarChart3,Eye,Heart,Bookmark,Copy,Users} from 'lucide-react';
export default function AnalyticsPage(){const [range,setRange]=useState('30d');const [stats,setStats]=useState<any>(null);const [loading,setLoading]=useState(true);useEffect(()=>{(async()=>{setLoading(true);const {data,error}=await supabase.rpc('get_creator_analytics',{p_window:range});if(!error)setStats(data?.[0]||data);setLoading(false)})()},[range]);const cards=[['Views',stats?.views||0,Eye],['Likes',stats?.likes||0,Heart],['Saves',stats?.saves||0,Bookmark],['Prompt Copies',stats?.prompt_copies||0,Copy],['Followers Gained',stats?.followers_gained||0,Users]];return <main className="min-h-screen bg-[#050609] p-4 pb-24 text-white md:pl-24"><div className="mx-auto max-w-6xl pt-8"><div className="flex flex-wrap items-center justify-between gap-3"><div><div className="text-xs font-black uppercase tracking-widest text-cyan-300">Creator Studio</div><h1 className="text-3xl font-black">Analytics</h1></div><div className="flex gap-2">{['7d','30d','90d','all'].map(x=><button key={x} onClick={()=>setRange(x)} className={`rounded-full px-4 py-2 text-xs font-bold ${range===x?'bg-cyan-400 text-black':'bg-white/5 text-white/50'}`}>{x.toUpperCase()}</button>)}</div></div>{loading?<div className="py-24 text-center text-white/40">Loading analytics…</div>:<><div className="mt-6 grid gap-3 sm:grid-cols-2 lg:grid-cols-5">{cards.map(([name,val,Icon]:any)=><div key={name} className="rounded-3xl border border-white/10 bg-white/[.035] p-5"><Icon className="h-5 w-5 text-cyan-300"/><div className="mt-5 text-2xl font-black">{Number(val).toLocaleString()}</div><div className="text-xs text-white/45">{name}</div></div>)}</div><div className="mt-4 grid gap-4 lg:grid-cols-2"><div className="rounded-3xl border border-white/10 bg-white/[.035] p-5"><h2 className="font-black">Rates</h2><div className="mt-4 space-y-4">{[['Prompt Copy Rate',stats?.copy_rate],['Save Rate',stats?.save_rate],['Engagement Rate',stats?.engagement_rate]].map(([n,v]:any)=><div key={n}><div className="flex justify-between text-sm"><span>{n}</span><b>{((Number(v)||0)*100).toFixed(2)}%</b></div><div className="mt-2 h-2 rounded-full bg-white/10"><div className="h-full rounded-full bg-cyan-300" style={{width:`${Math.min(100,(Number(v)||0)*100)}%`}}/></div></div>)}</div></div><div className="rounded-3xl border border-white/10 bg-white/[.035] p-5"><h2 className="font-black">Top content</h2><p className="mt-4 text-sm text-white/50">Top Reel: <b className="text-white">{stats?.top_reel_title||'—'}</b></p><p className="mt-3 text-sm text-white/50">Top Category: <b className="text-white">{stats?.top_category||'—'}</b></p><p className="mt-3 text-sm text-white/50">Top Tag: <b className="text-white">{stats?.top_tag||'—'}</b></p></div></div></>}</div></main>}
